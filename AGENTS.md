# Agent Instructions

## 项目信息
- 用途：深度学习学习代码（跟随 Andrej Karpathy 课程手写实现）
- 目录：`G:/学习课件/2026/8/nnlearn`
- 目录结构：
  - `micograd/`：micrograd 学习代码
    - `micograd.ipynb`：自动微分 `Value` 类实现（micrograd 风格）
    - `log.md`：micograd 部分的提交与修改记录
    - `README.md`：文件夹说明
  - `makemore/`：makemore 系列学习代码
    - `bigram.ipynb`：当前主力文件，bigram 名字生成模型
    - `log.md`：makemore 部分的提交与修改记录
    - `README.md`：文件夹说明
  - `neuron-net.ipynb`：当前为空
- GitHub 仓库：`https://github.com/dahuang546/personal-study-deep-learning.git`
- GitHub 用户名：`dahuang546`
- GitHub 认证方式：SSH（已通过认证）

## 约定
- 推送到 GitHub 时请使用 SSH 地址：`git@github.com:dahuang546/personal-study-deep-learning.git`，不要使用 HTTPS。
- `neuron-net.ipynb` 当前被忽略，不推送到 GitHub，等里面有内容后再决定。
- `micograd/` 部分内容暂时告一段落；后续提到“log”均指 `makemore/log.md`。
- 对 `makemore/` 相关内容进行推送时，记得在 `makemore/log.md` 追加一条内容简介记录。
- SSH 密钥保存在 `~/.ssh/id_ed25519.pub`（公钥）和 `~/.ssh/id_ed25519`（私钥），私钥不要外泄。
